<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Password</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .custom-shape {
            background-color: #fce4ec;
            border-radius: 15px;
        }
        .input-field {
            background: #ffffff;
            border-radius: 9999px;
            padding-left: 1rem;
        }
        .container {
            max-width: 500px;
            margin: 0 auto;
        }
    </style>
</head>
<body class="bg-pink-200 flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-lg rounded-lg flex flex-col container p-8 custom-shape">
        <h2 class="text-4xl font-bold text-gray-800 mb-8 text-center">Reset Password</h2>
        <form action="/newpassword" method="post">
            <div class="mb-6 relative">
                <input type="password" id="new-password" name="newpassword" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-gray-500 input-field" placeholder="Enter New Password">
            </div>
            <div class="mb-6 relative">
                <input type="password" id="con-password" name="confirmpassword" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-gray-500 input-field" placeholder="Enter confirm Password">
            </div>
            <button type="submit" class="w-full bg-orange-500 py-2 px-4 rounded-lg ">Change Password</button>
        </form>
    </div>
</body>
</html>