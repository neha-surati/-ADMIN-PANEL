<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .custom-shape {
            background-color: #fce4ec;
            border-radius: 15px;
        }
        .input-field {
            background: #ffffff;
            border-radius: 9999px;
            padding-left: 1rem;
        }
        .container {
            max-width: 500px;
            margin: 0 auto;
        }
    </style>
</head>
<body class="bg-pink-200 flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-lg rounded-lg flex flex-col container p-8 custom-shape">
        <h2 class="text-4xl font-bold text-gray-800 mb-8 text-center">Sign Up</h2>
        <form action="/signup" method="post">
            <div class="mb-6 relative">
                <input type="text" id="username" name="username" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-gray-500 input-field" placeholder="Enter your Username">
            </div>
            <div class="mb-6 relative">
                <input type="email" id="email" name="email" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-gray-500 input-field" placeholder="Enter your Email">
            </div>
            <div class="mb-6 relative">
                <input type="password" id="password" name="password" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-gray-500 input-field" placeholder="Enter your Password">
            </div>
            <div class="mb-6 relative">
                <input type="text" id="phone" name="phone" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-gray-500 input-field" placeholder="Enter your Phone">
            </div>
            <button type="submit" class="w-full bg-orange-500 py-2 px-4 rounded-lg ">Sign Up</button>
        </form>
        <p class="mt-4 text-center">Already have an account? <a href="/login" class="text-orange-600 hover:text-orange-800 hover-underline">Login here</a></p>
    </div>
</body>
</html>
